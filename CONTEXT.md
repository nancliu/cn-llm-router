# cn-llm-router · 项目上下文（CONTEXT）

> 本文件是仓库的领域文档入口（single-context 布局）。工程技能在探索代码前先读本文件与 `docs/adr/`。

## 项目是什么

cn-llm-router 是一个面向**国内大模型**的选择器/路由器：把用户的自然语言请求（或 Sub-Agent 子任务）识别为任务类别 + 复杂度，再依据"任务类别 × 模型"评分矩阵与性价比策略，推荐并路由到最合适的国产大模型。

与 RouteLLM（成本路由）、LiteLLM（网关）的定位区别：核心差异化是"中文任务分类 × 国内模型评分矩阵"，输出可解释推荐（主选 + 备选 + 理由），而非仅做省钱/网关转发。

## 核心决策（详见 docs/adr/0001）

- 任务分类：12 个大类 × 3 级复杂度（低/中/高）
- 评分：SuperCLUE 六维 × 任务权重 + 社区/人工评测信号 + 时效衰减；成本 = 输入×0.6 + 输出×0.4（元/百万 tokens）
- 选择策略三档：纯能力优先 / 平衡（默认）/ 性价比优先
- 数据交付物：打分表 v1-20260923（飞书表格，口径说明含来源 URL 清单与待补充项）

## 领域词汇表

- **任务类别**：12 个固定大类（程序编码、前端UX设计、文书撰写、数据分析、数学推理、Agent编排、长文档处理、翻译润色、多模态理解、知识问答/检索、创意生成、代码审查与调试）。扩展子类需同步更新评分矩阵。
- **复杂度**：低 / 中 / 高 三级，影响选择策略（低复杂度允许选性价比档模型）。
- **评分矩阵**：任务类别 × 模型的二维能力分（0-100），来源公开评测、可溯源。
- **性价比指数**：平均能力分 / 成本指数（成本指数 = 综合成本 / 最低综合成本）。
- **主选 / 备选**：推荐输出的一主一备 + 推荐理由。
- **模型注册表**：模型版本、价格、上下文、开源/闭源、细分型号的登记表（打分表 Sheet 1）。

### 路由层

**类别定义**：
12 个任务类别的唯一事实源（id/名称/判类说明/示例，`data/categories.yaml`），分类器 prompt、评分矩阵行、文档三处共享。
_Avoid_: 分类规则、类别表

**任务分类器**：
把自然语言请求识别为任务类别 + 复杂度的组件（LLM 结构化判类）。
_Avoid_: 意图识别、打标

**模型选择器**：
依据评分矩阵与策略档位，从注册表中确定性选出主选/备选的组件。
_Avoid_: 推荐引擎

**统一网关**：
把逻辑模型名映射到各厂商 API 的 OpenAI 兼容接入层。
_Avoid_: 代理、proxy

**逻辑模型名**：
选择器输出的、与厂商无关的模型标识（如 `deepseek-v4-pro`）。
_Avoid_: 模型ID、endpoint

**上游模型**：
各厂商实际提供的模型与端点，由统一网关按配置映射。
_Avoid_: 后端模型

**策略档位**：
选择器的三种模式：纯能力优先 / 平衡 / 性价比优先。
_Avoid_: 模式、开关

**供应商可用性**：
某 provider 的 API key 已配置（环境变量非空）的状态；选择器据此过滤"可用集"，默认只从可用集推荐。
_Avoid_: 在线状态、健康检查

## 文档结构

- `docs/adr/`：架构决策记录（轻量 ADR）
- `docs/agents/`：agent 工作约定（issue tracker / triage / domain）
- `AGENTS.md`：仓库代理约定入口（含 Agent skills 区块）
